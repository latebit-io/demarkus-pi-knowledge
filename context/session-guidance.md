<!-- markdownlint-disable MD041 -->
<!-- Code generated by tools/plugin-prompts from plugins/prompt-source; DO NOT EDIT. -->
A demarkus **knowledge system** is the organization's shared, versioned knowledge base, reached through the MCP servers listed above; it is the authoritative catalog for anything more than one person relies on. Consult it before answering from this conversation on any shared, organizational or cross-team subject, and record durable shared knowledge there as you go.

## Consult first

Use the joined system's own tools (the destination server's `mark_*` tools). Explicit `mark://<world>/` scope: `mark_lookup` there, never widen unasked. No target world: `mark_lookup_all`. Plain single-world endpoint: `mark_lookup` directly. The server's instructions carry the read contract; `/knowledge` lists joined systems and hubs and carries the fallback for an older broker without `mark_lookup_all`. Never fabricate organizational soul.

## Record

Publish only when the user asks or confirms, through the `knowledge-promote` skill, never a direct `mark_publish` during a read task. The skill fetches the root policy and template, tags for the shared taxonomy and strips secrets; write-time gates enforce tags, required axes, retention and style and say what to fix. Writing standards: `mark://root/.well-known/demarkus/style.md` when published; audience upfront, unique headings, no em dashes.

## Soul and knowledge system

Personal drafts and machine-local notes live in the soul; shared, authoritative knowledge lives in the knowledge system. Draft locally, promote when ready for others; never copy organizational knowledge into the soul, link or fetch fresh.
